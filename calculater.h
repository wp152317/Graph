double calculater(double arcs,char *str);       //calculating funtion(calculating)
int buildTree(char *str);                       //buildTree(graph)
double solveEquation(double x,double y);        //graph
